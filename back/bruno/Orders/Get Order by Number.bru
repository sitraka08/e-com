meta {
  name: Get Order by Number
  type: http
  seq: 4
}

get {
  url: {{baseUrl}}/api/orders/number/{{orderNumber}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

tests {
  test("Status is 200", function() {
    expect(res.status).to.equal(200);
  });
}
