meta {
  name: Get Profile
  type: http
  seq: 3
}

get {
  url: {{baseUrl}}/api/auth/profile
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

tests {
  test("Status is 200", function() {
    expect(res.status).to.equal(200);
  });
}
