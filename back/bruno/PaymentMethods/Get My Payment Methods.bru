meta {
  name: Get My Payment Methods
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/api/payment-methods
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

tests {
  test("Status is 200", function() {
    expect(res.status).to.equal(200);
  });

  test("Has payment methods array", function() {
    expect(res.body.data).to.be.an('array');
  });
}
