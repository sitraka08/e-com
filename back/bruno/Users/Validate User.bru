meta {
  name: Validate User
  type: http
  seq: 4
}

patch {
  url: {{baseUrl}}/api/users/{{userId}}/validate
  body: none
  auth: bearer
}

auth:bearer {
  token: {{adminToken}}
}

tests {
  test("Status is 200", function() {
    expect(res.status).to.equal(200);
  });
}
