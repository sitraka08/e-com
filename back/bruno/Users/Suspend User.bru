meta {
  name: Suspend User
  type: http
  seq: 5
}

patch {
  url: {{baseUrl}}/api/users/{{userId}}/suspend
  body: none
  auth: bearer
}

auth:bearer {
  token: {{adminToken}}
}

tests {
  test("Status is 200", function() {
    expect(res.status).to.equal(200);
  });
}
