meta {
  name: Delete Address
  type: http
  seq: 6
}

delete {
  url: {{baseUrl}}/api/addresses/{{addressId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

tests {
  test("Status is 200", function() {
    expect(res.status).to.equal(200);
  });
}
