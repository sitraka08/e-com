meta {
  name: Get My Addresses
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/api/addresses
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

tests {
  test("Status is 200", function() {
    expect(res.status).to.equal(200);
  });

  test("Has addresses array", function() {
    expect(res.body.data).to.be.an('array');
  });
}
